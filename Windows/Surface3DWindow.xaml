<Window x:Class="MGK_Analyzer.Windows.Surface3DWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        Title="{Binding WindowTitle, RelativeSource={RelativeSource Self}}" 
        MinWidth="960" MinHeight="600"
        Width="1200" Height="800"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        FontFamily="Malgun Gothic">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Syncfusion Surface Chart -->
        <syncfusion:SfSurfaceChart Grid.Column="0" x:Name="SurfaceChartControl" 
                                  Margin="10"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  ItemsSource="{Binding DataValues, RelativeSource={RelativeSource AncestorType=Window}}"
                                  XBindingPath="X" 
                                  YBindingPath="Y" 
                                  ZBindingPath="Z"
                                  RowSize="{Binding RowSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                  ColumnSize="{Binding ColumnSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                  Type="Surface"
                                  Rotate="43">
            <syncfusion:SfSurfaceChart.ColorBar>
                <syncfusion:ChartColorBar ShowLabel="True" DockPosition="Right" FontFamily="Malgun Gothic"/>
            </syncfusion:SfSurfaceChart.ColorBar>
        </syncfusion:SfSurfaceChart>
        
        <!-- Management Panel -->
        <Border Grid.Column="1" Width="280" Background="WhiteSmoke" 
                BorderBrush="LightGray" BorderThickness="1,0,0,0"
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch">
                <StackPanel>
                    <!-- 3D View Settings -->
                    <TextBlock Text="3D View Settings" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Rotation:" FontSize="10" 
                                  VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <Slider Grid.Row="0" Grid.Column="1" x:Name="RotationSlider"
                               Minimum="0" Maximum="360" Value="43"
                               TickFrequency="10" IsSnapToTickEnabled="True"
                               Margin="0,0,0,5" ValueChanged="RotationSlider_ValueChanged"/>
                    </Grid>
                    
                    <!-- Series Management -->
                    <Separator Margin="0,15,0,10"/>
                    <TextBlock Text="Series Management" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <ItemsControl ItemsSource="{Binding SeriesList, RelativeSource={RelativeSource AncestorType=Window}}"
                                 HorizontalAlignment="Stretch">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" 
                                       BorderBrush="#FFDDDDDD" 
                                       BorderThickness="1" 
                                       CornerRadius="3"
                                       Margin="0,2" Padding="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <CheckBox Grid.Column="0"
                                                  IsChecked="{Binding IsSelected}"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,8,0"/>
                                        
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding SeriesData.Name}" 
                                                       FontWeight="SemiBold" FontSize="11"/>
                                            <TextBlock FontSize="10" Foreground="Gray">
                                                <Run Text="{Binding SeriesData.Unit}"/>
                                                <Run Text=" - "/>
                                                <Run Text="{Binding SeriesData.SurfaceType}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        
                                        <Rectangle Grid.Column="2" Width="16" Height="16" 
                                                  VerticalAlignment="Center" 
                                                  Margin="5,0,0,0"
                                                  RadiusX="2" RadiusY="2">
                                            <Rectangle.Fill>
                                                <SolidColorBrush Color="{Binding SeriesData.Color}"/>
                                            </Rectangle.Fill>
                                        </Rectangle>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <!-- Control Buttons -->
                    <Separator Margin="0,15,0,10"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Add Sample" Click="AddSampleSurfaces_Click"
                               Padding="10,5" Margin="0,0,5,0"/>
                        <Button Content="Reset View" Click="ResetView_Click"
                               Padding="10,5" Margin="0,0,5,0"/>
                        <Button Content="Export" Click="ExportChart_Click"
                               Padding="10,5"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
